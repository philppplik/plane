[pytest]
# Die Tests analysieren die Quelldateien statisch; ein Importpfad wird nicht
# mehr gebraucht, seit die Reinigungslogik vollstaendig in Rust liegt.
testpaths = tests
addopts = -ra --strict-markers
